{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome for Snake Game Test",
            "url": "http://localhost:1024",
            "webRoot": "${workspaceFolder}/build"
        }
    ]
}
